cat < file1 > file2
cmd < stdin > stdout

cat > file1 < file2
cmd > stdout < stdin

cat < file1 < file2
cmd <'ignore'< stdin

echo "wtf" > file1 > file2
cmd > emptyfile > "wtf"


cat < file1 aqwe
cat aqwe 

echo hello | cat < txt
if redirection is being, pipe ignore

echo hellp > txt | cat
if redirection is being, pipe ignore

echo hello | cat
pipe work

echo hello |
>

cat < txt < txt <txt <txt
중복된 파일은 한 번만 들어간다.

cat < txt << end
end만 출력된다

< << < <
stdin redirection도 맨 마지막 fd만 가지고 가는 듯하다.
모든 파일 내용이 다 들어갔던 건 zsh이다.
